// Copyright (c) 2023, Kossivi and contributors
// For license information, please see license.txt

frappe.ui.form.on('Penalty', {
	// refresh: function(frm) {

	// }
});

/*frappe.ui.form.on("Penalty","onload", function(frm, cdt, cdn) { 
	var df = frappe.meta.get_docfield("Attendance Line","absence", cur_frm.doc.name);
    df.label = "Penalty amount";
	var df = frappe.meta.get_docfield("Attendance Line","sunday_hours", cur_frm.doc.name);
    df.hidden = 1;
	var df = frappe.meta.get_docfield("Attendance Line","hours_30", cur_frm.doc.name);
    df.hidden = 1;
	var df = frappe.meta.get_docfield("Attendance Line","hours_60", cur_frm.doc.name);
    df.hidden = 1;
	var df = frappe.meta.get_docfield("Attendance Line","night_hours", cur_frm.doc.name);
    df.hidden = 1;
});*/
